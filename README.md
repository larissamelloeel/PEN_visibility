# Projeto PEN

Visual em Streamlit.

## Como executar

```powershell
cd C:\PEN
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

O app abre a página principal com menu fixo, filtros locais e gráfico empilhado interativo.
